// GENERATED — nicht manuell bearbeiten, von scripts/dev/generate_frontend_environment.py
// aus config.ini erzeugt. Wird im Docker-Build von docker/frontend/Dockerfile
// automatisch aktualisiert, bevor `npm run build` läuft.
export const environment = {
  mqttWebsocketPort: 9001,
  mqttUsername: 'clawmachine',
  mqttPassword: 'claw_secret',
};
